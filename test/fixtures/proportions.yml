# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

variant2proportion_delete:
  variant: variant2proportion_delete
  composant: one(Ingredient)
  #composant_type: 'Ingredient'
  #composant_id: ingredients(:one).id
  value: 10000

proportions_type_1:
  variant: proportions
  composant: proportions_1(IngredientType)
  value: 7500

proportions_type_2:
  variant: proportions
  composant: proportions_2(IngredientType)
  value: 2500

proportions_1:
  variant: proportions
  composant: proportions_1(Ingredient)
  value: 8000

proportions_2:
  variant: proportions
  composant: proportions_2(Ingredient)
  value: 2000

proportions_3:
  variant: proportions
  composant: proportions_3(Ingredient)
  value: 10000

proportions_0:
  variant: proportions
  composant: proportions_0(Ingredient)
  value: 0

consume_stock_type:
  variant: consume_stock
  composant: one(IngredientType)
  value: 1000

consume_stock_one:
  variant: consume_stock
  composant: consume_stock_one(Ingredient)
  value: 1000
  
consume_stock_two:
  variant: consume_stock
  composant: consume_stock_two(Ingredient)
  value: 2000