<%= menu_item(menu: :nav_resume, active: params[:action] == 'index',
     label: 'Recettes', path: recipes_path)%>

<% if ['show', 'edit'].include?(params[:action]) %>
  <%= render 'nav_resume_base' %>
<% end %>

