<%= render 'nav_resume' %>
<%= render 'menu' %>
<%= render 'header' %>

<% content_for :item_content do %>

    <%= table_by_category(
    category: {
      name: :type,
      collection: @recipe_types
    },
    item: {
      collection: @recipes
    },
      columns: @columns
  ) %>

<% end %>
