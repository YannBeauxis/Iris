<% if can?(:update, @recipe) then %>
  <%= menu_item(menu: :content_menu, 
       path: edit_recipe_path(@recipe), label: 'Modifier') %>
<% end %>

<%= menu_item(menu: :content_menu, 
     path: recipe_path(@recipe), label: 'Supprimer', 
     link_param: {method: :delete, data: { confirm: 'Voulez-vous supprimer cet utilisteur ?' }}) %>
