<%= menu_item(menu: :nav_resume, active: params[:action] == 'index',
     label: 'Recettes', path: recipes_path)%>

<%= render 'recipes/nav_resume_base' %>

<% if params[:action] != 'new' %>
  <%= menu_item(menu: :nav_resume, active:true, label: @variant.name) %>
<% end %>
