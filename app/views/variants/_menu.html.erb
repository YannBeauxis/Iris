<% content_for :content_menu do %>
  > <%= link_to 'Modifier', edit_recipe_variant_path(@recipe, @variant) %><br>
  > <%= link_to 'Supprimer', recipe_variant_path(@recipe, @variant),
              method: :delete,
              data: { confirm: 'Voulez-vous supprimer cette variante ?' } %>
<% end %>
